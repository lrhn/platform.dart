name: platform
version: 4.0.0
description: A plugable, mockable platform abstraction for Dart.
homepage: https://github.com/google/platform.dart

environment:
  sdk: ^3.0.0

dependencies: 
  meta: ^1.7.0

dev_dependencies:
  dart_flutter_team_lints: ^1.0.0
  test: ^1.16.8
